### Changed

- Refreshed the `repo-template` snapshot to `main` commit `cd79b1c`, carrying
  the staged/worktree pre-commit guard from repo-template#114 so newly
  onboarded or repaired repos inherit the formatter-drift block before commit.
