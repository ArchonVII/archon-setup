- **Onboarding-emitted check-map now derives `defaults.stack` from the installed gate caller.** `writeCheckMap` rewrites the repo-template snapshot's `defaults.stack` to the single active `stack:` value in the vendored `repo-required-gate.yml` caller (node today, post-#284), and `--audit` reuses the same renderer, so the documented path-to-check contract can no longer disagree with the CI stack the gate actually runs. (#293)
