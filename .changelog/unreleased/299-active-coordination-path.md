### Added

- The opt-in coordination-board feature now installs an explicit
  active-coordination path: `.agent/coordination/board.md` plus a live
  `claims/` location (`writeCoordinationClaims`) that `agent:status` reports as
  installed. (#299)
