- **`tighten-required-gate` now works from baseline branch protection.** When protection exists with `required_status_checks: null` (the shipped baseline), the subresource PATCH 404s ("Required status checks not enabled") and was misreported as missing branch protection. The command now routes that state through a full branch-protection PUT that enables the gate while preserving every current setting (reviews, restrictions, linear history, admin enforcement, ...), reports `via: full-protection-put`, and a PATCH-time 404 now gets its own `required-checks-not-enabled` status + rerun hint instead of the wrong "apply baseline protection first" advice. (#301)
