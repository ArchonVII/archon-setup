- **archon-setup now runs its own repo-required-gate.** New `.github/workflows/repo-required-gate.yml` caller (stack: node, ci:full label escape, concurrency-cancel) produces the `repo-required-gate / decision` check the close tooling expects, replacing the missing required-gate prerequisite that forced `--skip-ci-guard` during promotions; standalone Node CI drops to push:main only since the gate runs the same reusable lane on PRs; `.agent/check-map.yml` declares the new gate. The ready-wrapper flag cleanup lands upstream in repo-template and flows to archon-setup through the next snapshot refresh. (#302)
