- **archon-setup now runs its own repo-required-gate.** New `.github/workflows/repo-required-gate.yml` caller (stack: node, ci:full label escape, concurrency-cancel) produces the `repo-required-gate / decision` check the close tooling expects, ending the standing `--skip-ci-guard` escape on promotions; standalone Node CI drops to push:main only since the gate runs the same reusable lane on PRs; `.agent/check-map.yml` declares the new gate. (#302)
