- **`repo-required-gate / decision` is now a required status check on archon-setup `main`.** `tighten-required-gate` ran after #325 landed (that PR's gate run satisfied GitHub's ran-within-7-days requirement); the manifest `postChecks` entry is recorded complete. Merges to `main` without a passing gate are now blocked by branch protection, ending the standing `--skip-ci-guard` escape for good. (#302)
