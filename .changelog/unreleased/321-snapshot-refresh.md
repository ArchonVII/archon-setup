- **Provider snapshots re-pinned to the post-friction-arc mains.** github-workflows `990cbca` (substance-only pr-contract #100, example-caller concurrency #102) and repo-template `c19e745` (re-vendored validator + parity guard #139, caller concurrency + `ci:full` escape #141); self-apply synced the root callers and lifecycle scripts so archon-setup's own CI gets the concurrency-cancel and the relaxed contract mechanically. (#321)
