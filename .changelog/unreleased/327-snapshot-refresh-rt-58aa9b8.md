- **repo-template snapshot re-vendored @ 58aa9b8; onboarding now ships the docs system and the check-map-aware close scripts.** Picks up repo-template#143 (close:ci:guard / close:scan:complete honor `.agent/check-map.yml` `required_gate.check_name`) and repo-template#124 S1 (doc-map spine, scripts/docs generators, docs:render drift gate). `.agent/doc-map.yml` and `docs/agent-process/doc-system.md` added to the snapshot copy list so the vendored llms.txt/scripts never reference files onboarding does not install. Self-apply refreshed archon-setup's own lifecycle + close scripts from the new capture. (#327)
