- **Deterministic onboarding contract and audit gate.** Added `docs/agent-process/deterministic-onboarding.md` to define "fully onboarded" as audited default-branch state, document which onboarding steps must be automated, and separate explicit human/agent decisions from repeatable tool-owned work. `onboard --audit` now emits `audit.onboardingCompletion` and prints an `Onboarding completion` line so missing required anchors, incomplete startup readiness, or missing/drifted selected baseline items block any complete-onboarding claim. (#340)
