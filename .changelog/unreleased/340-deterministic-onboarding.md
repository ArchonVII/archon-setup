- **Deterministic onboarding contract and audit gate.** Added `docs/agent-process/deterministic-onboarding.md` to define "fully onboarded" as current default-branch state with a clean post-merge audit, document which onboarding steps must be automated, and separate explicit human/agent decisions from repeatable tool-owned work. `onboard --audit` now emits `audit.onboardingCompletion` and prints an `Onboarding completion` line so missing required anchors such as `AGENTS.md` or `.github/archon-setup.json` block any complete-onboarding claim. (#340)
