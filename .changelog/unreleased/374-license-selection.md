### Fixed

- Honor explicit headless feature selections, including empty selections, and
  stop `foundation.git-init` from silently re-adding an excluded
  `foundation.license` capability and MIT license file.
