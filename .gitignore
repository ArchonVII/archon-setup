node_modules/
*.log
.DS_Store
.archon-tmp/
dist/
.env
.env.local

# agent/onboarding runtime metadata
.agent/current-task.json
.agent/close-scan/
.archon/events.jsonl

# Agent-local .claude state is ignored except the friction ledger, which the
# owner-maintenance lane lets agents append to directly on main.
.claude/*
!.claude/friction.md
