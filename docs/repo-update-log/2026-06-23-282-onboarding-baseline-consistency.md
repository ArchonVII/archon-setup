# 2026-06-23 - Onboarding baseline internal-consistency fixes (#278/#280/#281/#282)

- **Issue/PR:** #278, #280, #281, #282 / (pending)
- **Branch:** claude/fervent-hopper-ve9ekg
- **Changed paths:** `src/server/tasks/writeAgentsMd.mjs`, `src/server/tasks/writeAgentLifecycle.mjs`, `src/server/tasks/writeFrictionLedger.mjs`, `src/server/tasks/writeGitignore.mjs`, `src/registry/features.json`, `scripts/refresh-snapshots.mjs`, `scripts/close/scan-complete.mjs`, `src/snapshots/repo-template/scripts/close/scan-complete.mjs`, `src/snapshots/repo-template/docs/agent-process/message-protocol.md` (new), `src/snapshots/github-workflows/repo-required-gate.yml`, `src/snapshots/manifest.json`, `test/{writeAgentsMd,writeAgentLifecycle,writeFrictionLedger,writeGitignore,agentLifecycleScripts,manifestAccuracy}.test.mjs`, this fragment + the changelog fragment.
- **What changed:** Closed the onboarding-completeness cluster so a freshly onboarded repo satisfies its own baseline:
  - **#278** — `writeAgentsMd` (foundation.agents) now distributes `docs/agent-process/message-protocol.md` alongside the other `docs/agent-process/` charters, and `refresh-snapshots` mirrors it into the snapshot. The charter already existed upstream in `repo-template`; nothing distributed it, so every onboarded repo carried the dangling `AGENTS.md` -> `message-protocol.md` link doc-health reports.
  - **#280** — the installed `repo-required-gate.yml` scaffold (sourced from the `github-workflows` `examples/` snapshot, not `repo-template`) defaulted to `stack: minimal`, which the gate classifier hard-rejects once package/code files are touched — true of every baseline repo. The scaffold now defaults to `stack: node`.
  - **#281** — the same scaffold ran `dependency-review` by default, which hard-fails on a repo without GitHub Dependency Graph enabled. It now defaults `run-dependency-review: false` with inline opt-in instructions (enable Dependency Graph, then flip to true).
  - **#282** — `writeAgentLifecycle` installs + wires the `agent:close-preflight` / `agent:pr-ready` wrappers `AGENTS.md` mandates; `writeFrictionLedger` re-includes `.claude/noticed.md` / `.claude/napkin.md`; `writeGitignore` ignores `.agent/current-task.json`; and `scripts/close/scan-complete.mjs` skips `node-test` when no `test` script exists.
- **Verification:** `node --test "test/*.test.mjs"` — 586/589 pass, 2 pre-existing skips, and 1 pre-existing unrelated failure (`runPreflight surfaces originDetected`, an https-remote/preflight detection issue that also fails on the clean tree, untouched here). Affected suites green: writeAgentLifecycle, writeFrictionLedger, writeGitignore (new), writeAgentsMd, agentLifecycleScripts, auditAgentLifecycle, manifestAccuracy. `npm run snapshots:verify` (with provider checkouts) — ok githubWorkflows @ 71d7e64, repoTemplate @ 896082d, orgDefaults @ fe48c2f. Provider suites: github-workflows vitest 159/159; repo-template close-scan 14/14.
- **Propagation:** the canonical fixes live upstream — `github-workflows` `examples/repo-required-gate.yml` (#280/#281) and `repo-template` `scripts/close/scan-complete.mjs` (#282); both committed on `claude/fervent-hopper-ve9ekg`. The archon-setup snapshots are updated in lockstep and the manifest pins bumped to those integration-branch commits so fresh onboards get the fixes now and `snapshots:verify` stays green; once the provider branches merge to `main` / the `v1` tag is re-cut, a normal `npm run refresh-snapshots` reconciles the pins.
