# 2026-06-23 - Wire doc-health + repo-update-log README into onboarding

- **Issue/PR:** none / pending (direct request)
- **Branch:** claude/zen-sagan-xexvdp
- **Changed paths:** `.changelog/unreleased/doc-health-onboarding-feature.md`, `src/registry/features.json`, `src/server/executor/executePlan.mjs`, `src/server/onboard/auditPlan.mjs`, `src/server/tasks/writeAgentsMd.mjs`, `src/server/tasks/writeDocHealth.mjs`, `test/manifestAccuracy.test.mjs`, `test/onboardHeadless.test.mjs`, `test/registry.test.mjs`, `test/writeAgentsMd.test.mjs`, `test/writeDocHealth.test.mjs`, `docs/repo-update-log/2026-06-23-doc-health-onboarding-feature.md`.
- **What changed:** Closed a startup-readiness gap: the `2026-06-15-document-policy` startup baseline required `scripts/doc-health/{lib,health}.mjs`, `docs/agent-process/doc-health.md`, and `docs/repo-update-log/README.md`, but no onboarding feature generated them, so a fresh onboard always reported startup readiness "incomplete" until the files were hand-copied. Added a locked-default `agent-workflow.doc-health` feature + `writeDocHealth` task (mirrors `agent-workflow.doc-sweep`), registered it in the executor, and extended `foundation.agents`/`writeAgentsMd` to distribute `docs/repo-update-log/README.md`. Extended the onboard audit (`auditPlan`) with expected-body and startup-readiness staleness handling for all four files.
- **Verification:** `npm test` passed 592/595 (2 skipped; the 1 failure — `checkOriginRemote.test.mjs` "runPreflight surfaces originDetected for a github target" — is pre-existing and reproduces on a clean tree, an environment artifact of the sandbox git SSH-URL rewriting, unrelated to this change). End-to-end CLI check: a fresh `node bin/onboard.mjs <dir> --owner ArchonVII --repo <name>` (with the offline license/gitignore seams) now reports `Startup readiness: complete` and `49 present, 0 missing, 0 drifted`.
- **Propagation:** The `repo-template` snapshot already carries these files (landed via #225's `refresh-snapshots`); this lane only wires them into the onboarder. No snapshot, workflow, or `agent:self-apply` surface changes. The separate `docs/agent-process/message-protocol.md` dangling-link warning (AGENTS.md references a doc not in the snapshot) is tracked in its own issue.
