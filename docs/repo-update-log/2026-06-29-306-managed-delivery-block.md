# 2026-06-29 - #306 Managed delivery-workflow block

- **Changed paths:** `src/server/onboard/auditPlan.mjs`, `src/server/tasks/writeAgentsMd.mjs`, `test/onboardAudit.test.mjs`, `test/writeAgentsMd.test.mjs`, `.changelog/unreleased/306-managed-delivery-block.md`, `docs/repo-update-log/2026-06-29-306-managed-delivery-block.md`.
- **What changed:** Onboarding now writes a managed `delivery-workflow` block into the generated `AGENTS.md`, guaranteeing every onboarded repo carries the cross-tool issue -> branch -> atomic-commit -> PR delivery contract in a re-syncable managed region. `onboard --audit` now flags the block's absence so drifted or older repos are detected. The onboarding changelog default is switched to Mode 2 (fragment-based) to match the shipped close guard. Addresses #306 and #291.
- **Verification:** `test/writeAgentsMd.test.mjs` asserts the managed delivery-workflow block is emitted and re-syncable; `test/onboardAudit.test.mjs` asserts audit flags its absence and the Mode 2 changelog default. GitHub required checks pass on PR #311 (`repo-required-gate / node ci`, `pr contract`, `decision`, and the `check / check` repo-update-log gate). The fragment commit is docs-only.
