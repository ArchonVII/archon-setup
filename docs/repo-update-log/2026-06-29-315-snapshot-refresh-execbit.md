# 2026-06-29 - #315 Snapshot refresh + .githooks exec bit

- **Changed paths:** `scripts/refresh-snapshots.mjs`, `src/snapshots/manifest.json`, `src/snapshots/repo-template/**` (re-vendored from repo-template `4ddf930`), `scripts/agent/lib.mjs`, `scripts/agent/start-task.mjs`, `scripts/agent-pr-ready.mjs`, `scripts/close/scan-complete.mjs`, `scripts/doc-sweep/git.mjs` (root baseline re-applied from the snapshot), `test/snapshotHookExecBit.test.mjs`, `test/close-scan.test.mjs`, `.changelog/unreleased/315-snapshot-refresh-execbit.md`, `docs/repo-update-log/2026-06-29-315-snapshot-refresh-execbit.md`.
- **What changed:** Re-vendored the `repo-template` snapshot from the merged baseline (`896082d` -> `4ddf930`), propagating #290 (AGENTS.md), #292/#295 (`start-task`/`lib`/`doc-sweep`/`pr-ready`), and #286 (the `decideNodeTest` close-scan fix). To close #314, `refresh-snapshots.mjs` now stamps the executable bit (`git update-index --add --chmod=+x`) onto the vendored hook entrypoints (`.githooks/{commit-msg,pre-commit}` + `.githooks/scripts/*.sh`) after copying, because the refresh runs on Windows (`core.filemode=false`) where `git add` records new hook files as `100644`. The snapshot now ships those eight hooks at `100755`, so an onboarded repo's hooks are not silently skipped on Unix. The onboarder's `writeGithooks` already `chmod 0o755`s on apply, so the fix is defense-in-depth end-to-end. The root agent baseline was brought current with the refreshed snapshot via `npm run agent:self-apply` (never hand-edited), which applied the #286 `decideNodeTest` change to archon-setup's own root `scripts/close/scan-complete.mjs` in lockstep.
- **Verification:** `npm run snapshots:verify` clean (repoTemplate @ `4ddf930`, 120 files; no github-workflows/org-defaults churn). `npm test` green. `git ls-files -s src/snapshots/repo-template/.githooks` shows `100755` for all eight hook entrypoints. New regression tests: `test/snapshotHookExecBit.test.mjs` (pins the `100755` index modes, #314) and `test/close-scan.test.mjs` (pins `decideNodeTest` absent/unparseable/present behavior, #286). `npm run agent:self-apply -- --check` reports no drift.
