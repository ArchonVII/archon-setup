# 2026-07-01 - #301 tighten-required-gate handles required_status_checks: null

- **Changed paths:** `src/server/branchProtection/tightenRequiredGate.mjs`, `test/tightenRequiredGate.test.mjs`, `.changelog/unreleased/301-tighten-gate-null-rsc.md`, this fragment.
- **What changed:** Baseline branch protection ships `required_status_checks: null`; GitHub 404s the `/protection/required_status_checks` subresource in that state, so `tighten-required-gate` PATCHed into a 404 and misreported `missing-protection` ("apply baseline branch protection first") even though protection existed — the lifeloot onboarding failure in the issue. The command now detects the null state from the protection read and enables the gate through a full-protection PUT built by the new exported `putBodyFromProtection()` (maps GitHub's GET shape to the PUT shape, preserving enforce_admins, review rules incl. dismissal/bypass actor groups, restrictions, and all optional booleans so nothing silently resets). Results carry `via: "required-status-checks-patch" | "full-protection-put"`. A PATCH-time 404 (checks disabled between read and write) now reports the distinct `required-checks-not-enabled` status with a rerun hint. The pre-fix test asserted the wrong behavior (mock let the null-state PATCH succeed) — rewritten to pin the PUT path, the preserved-settings body, and the distinct 404 status.
- **Verification:** `node --test test/tightenRequiredGate.test.mjs` → 5/5; full suite `npm test` → green (639 baseline + new cases, 0 fail) on the lane worktree.
- **Propagation:** none — repo-local CLI behavior. Unblocks the owner-gated #302 (archon-setup self-gate install) and the hudson-bend protection gap flagged in hudson-bend#295.
