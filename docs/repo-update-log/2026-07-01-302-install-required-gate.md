# 2026-07-01 - #302 Install the repo-required-gate caller; PR CI moves inside the gate

- **Changed paths:** `.github/workflows/repo-required-gate.yml` (new caller, copied from repo-template's post-#141 shape, `stack: node`), `.github/workflows/node-ci.yml` (standalone caller → push:main only), `.agent/check-map.yml` (`required_gate` → `repo-required-gate / decision` + workflow path), fragments.
- **What changed:** archon-setup's close tooling expects `repo-required-gate / decision` but the repo never produced it, so every promotion needed the documented `--skip-ci-guard` escape (#302 evidence comment). This installs the gate caller (pr-contract + path-classified lanes + single `decision` aggregate). The gate's node lane runs the same `node-ci.yml@v1` reusable at PR time (no lockfile → it auto-falls back to `npm install --no-package-lock`), so the standalone Node CI caller drops its `pull_request` trigger to avoid the double-run hudson-bend#295 already eliminated. Check-map now declares the gate the repo actually produces.
- **Verification:** actionlint clean on both touched workflows (local binary). Full `npm test` on the lane worktree: 639 pass / 0 fail / 2 skipped. Live proof on this PR itself: `repo-required-gate / decision` appears and passes on the PR head (the caller ships in the PR, so the gate runs on it).
- **Propagation:** none upstream (repo-local caller wiring). Post-merge follow-ups tracked on #302: after this and any open PRs merge, run `node bin/archon-setup.mjs tighten-required-gate --owner ArchonVII --repo archon-setup` to make the gate a required status check (needs the check to have run within 7 days — this PR's run satisfies that); repo-template#143 fixes the vendored guard/scan to honor the check-map for consumers with custom gates.
