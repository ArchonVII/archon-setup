# 2026-07-02 - #327 Re-vendor repo-template @ 58aa9b8 (close-gate fix + docs system S1)

- **Changed paths:** `scripts/refresh-snapshots.mjs` (copyFiles += `.agent/doc-map.yml`, `docs/agent-process/doc-system.md`, source-commented), `src/snapshots/repo-template/**` + `src/snapshots/manifest.json` (machine-written mirror, pin c19e745 → 58aa9b8, 130 files), self-applied root files (`scripts/close/{lib,scan-complete,ci-guard}.mjs`, `scripts/agent*`, `scripts/pr-contract.mjs`, `package.json` merge), `docs/ecosystem-overview.md` (regenerated), fragments.
- **What changed:** repo-template main moved with two landings the vendored mirror must track: repo-template#143 (d012f83 — close guard/scan honor the check-map `required_gate.check_name`) and repo-template#124 S1 (58aa9b8 — doc-map spine + scripts/docs generators). The scripts flowed automatically via `copyDirs:"scripts"`, but the refreshed llms.txt nav references `.agent/doc-map.yml` and `docs/agent-process/doc-system.md`, which were not in the copy list — onboarded repos would carry dangling references (the archon-setup#278 failure mode) and the vendored `scripts/docs/render.mjs` would have no spine to read. Both added with source comments; refresh re-run with `--accept-snapshot-divergence` (config newer than capture, the documented case).
- **Verification:** `npm run snapshots:verify` → ok githubWorkflows @ 990cbca (20), ok repoTemplate @ 58aa9b8 (130), ok orgDefaults @ fe48c2f (1). Full `npm test` on the lane: 639 pass / 0 fail / 2 skipped. `npm run agent:self-apply -- --check` → all targets already-done after apply.
- **Propagation:** known upstream gap logged to anomaly triage: repo-template's `.gitignore` gained a `docs/STATUS.md` ignore rule in #124 S1, but `.gitignore` cannot ship in the npm snapshot (npm pack strips it) and the onboarding gitignore lifecycle (#304) does not yet write that rule — onboarded repos running `docs:status` could commit the rendered dashboard. Follow-up belongs in the onboarding lifecycle, not this refresh.
