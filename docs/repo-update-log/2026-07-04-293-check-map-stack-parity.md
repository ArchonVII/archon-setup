# 2026-07-04 - #293 Check-map defaults.stack derived from the gate caller

- **Issue/PR:** #293 / pending
- **Branch:** `agent/claude/293-check-map-stack-parity`
- **Changed paths:** `src/server/tasks/writeCheckMap.mjs`, `src/server/onboard/auditPlan.mjs`, `test/checkMapStackParity.test.mjs`, fragments.
- **What changed:** Onboarding shipped `.agent/check-map.yml` with `defaults.stack: minimal` (byte-copied from the repo-template snapshot) while the installed `repo-required-gate.yml` caller defaults `stack: node` (post-#284), so the documented path-to-check contract lied to any agent predicting checks. `writeCheckMap.apply()` now renders the check-map body by rewriting `defaults.stack` to the single active `stack:` line extracted from the vendored gate-caller snapshot (line-anchored, comment- and CRLF-tolerant, throws loudly on zero/multiple matches), and `auditPlan` reuses the same `loadCheckMapBody()` renderer so a freshly onboarded repo audits as present, never drifted. No vendored snapshot was touched; a future snapshot refresh that changes the caller stack automatically carries into the emitted check-map. `verify()` deliberately keeps only the gate-reference assertion so owner-customized consumer stacks do not fail `--audit` re-runs.
- **Verification:** `node --test test/checkMapStackParity.test.mjs` -> 8 pass / 0 fail (TDD: the emitted-vs-caller parity test failed `'minimal' !== 'node'` before the fix). `npm test` -> 650 tests, 648 pass / 0 fail / 2 skipped (baseline 640/0/2 + 8 new). `npm run snapshots:verify` -> ok githubWorkflows @ 990cbca, repoTemplate @ 58aa9b8, orgDefaults @ fe48c2f. Idempotency covered in-suite: second run reports `already-done` and rewrites nothing.
- **Propagation:** already-onboarded repos keep their repo-owned check-map (backfill tracked under epic #307 Phase F, not this PR). Upstream `repo-template/.agent/check-map.yml` still says `stack: minimal` for clone-consumers — worth an upstream follow-up, but the derivation here stays correct regardless of that value.
