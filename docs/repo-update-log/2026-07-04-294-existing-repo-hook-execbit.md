# 2026-07-04 - #294 Stage hook exec bits on the existing-repo onboarding path

- **Changed paths:** `src/server/tasks/writeGithooks.mjs` (shared `stageHookExecBits` export, git-aware `check()`/`apply()`/`verify()`), `src/server/tasks/initGitAndCommit.mjs` (consumes the shared helper), `test/onboardHookExecBitExistingRepo.test.mjs` (new), `test/onboardHookExecBit.test.mjs` (review round: truly-fresh mutation guard), `.changelog/unreleased/294-existing-repo-hook-execbit.md`, this fragment.
- **What changed:** Closes the existing-repo hole the #294 verification pass (issue comment, 2026-07-04) proved was left open by #317: `initGitAndCommit.check()` returns `already-done` for any target with prior commits, so the bootstrap-path exec-bit staging never ran on the path every real onboarding (civ-sim, lifeloot) took, and hooks were later committed at `100644` from Windows hosts. `stageHookExecBits` moved verbatim into `writeGithooks.mjs` as the single shared implementation (bootstrap behavior byte-identical); `writeGithooks.apply()` now stages `git update-index --add --chmod=+x` when the target already has `.git` (staging only — onboarding never auto-commits on user history; fresh onboards skip because no `.git` exists yet); `check()` returns `needs-apply` when a hook is tracked at `100644` so re-onboard/update runs repair the drift; `verify()` additionally checks the staged mode of tracked hooks, which makes it honest on win32 where the disk-mode probe is a no-op. The staged-mode probes run on every platform, not just win32 — a platform gate would leave Unix `core.filemode=false` hosts unrepaired and make the regression test platform-dependent.
- **Verification:** Red→green on this Windows host: at `main` (00e222e) both new tests fail (`'.githooks/commit-msg must be staged executable (got untracked)'`; `check()` returns `already-done` after a forced `100644` degrade). With the fix, a real `runOnboard` into a temp repo pre-initialised with a prior commit and `core.filemode=false` stages all eight entrypoints at `100755` (`git ls-files -s`) with HEAD unchanged, and the degrade→repair cycle passes. Full suite in the lane worktree: 645 tests / 643 pass / 0 fail / 2 skipped (main baseline 642/640/0/2 — exactly the three new tests). `npm run snapshots:verify` → ok ×3.
- **Review round (adversarial adjudication):** the #294 refactor silently defanged the #317 bootstrap regression guard — deleting `await stageHookExecBits(cwd)` from `initGitAndCommit.apply()` survived the whole suite (mutation verified at `279a318`: the pre-init #317 test lets `writeGithooks.apply()` stage at phase 0 because `.git` already exists, and index mode is sticky through the later `git add --all`). Added a truly-fresh variant to `test/onboardHookExecBit.test.mjs` that starts with no `.git` at all and simulates the filemode-less host via `GIT_CONFIG_COUNT`/`GIT_CONFIG_KEY_0`/`GIT_CONFIG_VALUE_0` (command-scope config, outranks the `core.filemode` a fresh `git init` auto-detects), so the bootstrap staging call is the only possible source of `100755`; the mutation now fails that test (kill re-verified). Also corrected the `firstBadStagedMode` doc comment, which claimed apply() covers untracked hooks: disk-present-but-untracked residue actually short-circuits `check()` to `already-done` (repair takes one extra run) — deliberately in the #294 spec scope (verification comment item 3), now documented as the residual gap it is instead of claimed coverage.
- **Propagation:** none via `update` (onboarding-engine behavior). Consumer residue — civ-sim/lifeloot/hudson-bend already carrying committed `100644` hooks — stays with epic #307 Phase F (owner-gated backfill). Upstream belt-and-braces candidate (chmod in repo-template `install-githooks.sh`) noted in the #294 verification comment; `HOOK_FILES` hardcoded-list gap documented there and out of scope.
